# sddm-qdots config file
# is ment to be copied to dir:
# /etc/sddm.conf.d/
# by install-sddm script

# Name of session file for autologin session (if empty try last logged in)
Session=xinitrc

# Username for autologin session
User={{ .chezmoi.username }}

[Theme]
# Current theme name
Current=chili
