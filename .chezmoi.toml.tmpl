{{- $email := promptString "Enter email address (for git):" -}}
{{- $gituser := promptString "Enter git user name:" -}}
{{- $qdotsprompt := promptString "Choose qdots prompt type [wsl, simple]:" -}}
[data]
	email = "{{ $email }}"
	gituser = "{{ $gituser }}"
	qdotsprompt = "{{ $qdotsprompt }}"

